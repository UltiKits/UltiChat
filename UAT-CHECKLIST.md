# UltiChat — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the
steps to exercise it and the observable truth that proves it works. It is an internal reference
for real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`.

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only,
  as `.neg-<slug>` — a negative case still tests the same feature, so the base ID is unchanged.
- **Layer**, copied verbatim from Laojun's own `ultitools-real-client-uat` skill so no
  translation step exists at dispatch time: `protocol`, `java-client`, `os-input`, `pixel`,
  `server`, `human`.
- **Human-authenticated-session rows (D-27b):** a row whose Steps can only be exercised through
  the maintainer's own authenticated UltiCloud panel session — because the real inbound WebSocket
  route requires a valid cloud token issued solely by a personal magic-link login, not by any
  credential or route the executing agent could construct itself — carries the fixed
  Preconditions phrase `maintainer-authenticated UltiCloud panel session (personal credentials)`
  (appended to, not replacing, the row's own preconditions) and Layer `human`. Such a row ends at
  `human-uat-pending` by design — the second legitimate exit named in the header blockquote — not
  as a workflow failure discovered mid-dispatch. UltiChat has no panel-capability rows of its own,
  so no row below is currently affected; the convention is stated here for template consistency
  with the framework's checklist.
- **Expected** must name an observable truth — an exact chat line, a log line, a database row,
  an inventory slot — and never the words "it works".
- **Covers** back-references a Phase 9 GUI-excluded class name; left blank when no such class
  applies. UltiChat is not one of the nine modules in Phase 9's GUI-exclusion register (confirmed
  by reading `.planning/phases/09-module-ecosystem-readiness-and-test-coverage/gui-exclusions/` —
  no `UltiChat.md` file exists there, and no file in that directory names UltiChat), so every row
  below leaves `Covers` blank.
- A row whose Preconditions name a prior row must appear after that row in file order — asserted
  mechanically: for every row, every checklist ID cited in its Preconditions cell must have a
  strictly smaller line number in this file than the row citing it (sweep class 8, D-27a).
- **Config-per-file rule (D-06):** one checklist row per `@ConfigEntity`-annotated class or per
  shipped yml file, never one row per key. This is the one deliberate exception to the "ID cites
  its `FEATURES.md` ID verbatim" rule above: a config-per-file row (ID suffixed `-yml`, e.g.
  `ultichat.config.chat-yml`) aggregates every per-key `ultichat.config.<file-stem>.*` row for
  that file rather than citing a single one of them — there is no single per-key ID to cite when
  the row's job is exercising the whole file at once. A build-time (Maven-filtered) property —
  one resolved by Maven resource filtering at package time, such as `env.yml`'s `api-url` — is
  exercised by the build that produced the handed-over jar, not by editing the shipped file after
  the fact; such a row must state which build property it proves, and its Preconditions must
  state that the file it reads comes from a fresh, unmodified extraction of that build's jar at
  the property's shipped default, not a hand-edited copy. `env.yml` ships with the framework, not
  with UltiChat; UltiChat ships no Maven-filtered config file of its own, so no row below is
  currently affected.
- This repository's shipped default is `language: "zh"` in `config.yml`. Every row below whose
  Expected quotes a literal in-game or console line therefore carries the precondition
  `language: en` set in `plugins/UltiTools/config.yml`, so the observed line matches this
  document's English-only text exactly, character for character — **except every anti-spam
  refusal line** (`ultichat.chat.pipeline.neg-antispam-block`, and the anti-spam slice of
  `ultichat.config.chat-yml`), which is hardcoded Simplified Chinese regardless of the `language`
  setting; a known product defect, `UltiKits/UltiChat#18`, not fixed here per this plan's
  zero-new-code rule. Those rows quote the actual (Chinese) text, not the unused `lang/en.json`
  key text, deliberately.
- `plugin.i18n(...)` in this framework is a raw dictionary lookup with **no** `MessageFormat`
  processing — a doubled single-quote in the JSON source (`''`) is not an escape sequence and
  reaches the player as two literal apostrophe characters, not one. Every Expected line below
  that quotes an i18n-templated message preserves the doubled quote exactly as written in
  `lang/en.json`/`lang/zh.json`, rather than "correcting" it to a single apostrophe.

## Administration

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultichat.uchat.reload | `language: en` in config.yml; edit `config/chat.yml`'s `chat.format` to a distinguishable value on disk (do not use `/uchat reload` yet) | Run `/uchat reload`, then send a chat message | Console/chat shows `UltiChat configuration reloaded.` (green, `config_reloaded`); the sent chat message is now formatted using the edited `chat.format` value, not the previous one | server | |

## Auto-Reply

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultichat.autoreply.add | `language: en` in config.yml; a rule name `greeting` does not currently exist — **establish this, do not assume it**: since `UltiKits/UltiChat#17` a `greeting` added by an earlier execution of this checklist is on disk and outlives a reload and a restart alike, so run `/uchat autoreply list` and, if it is listed, `/uchat autoreply remove greeting` before starting | Run `/uchat autoreply add greeting hello` | Chat/console line reads `Auto-reply rule ''greeting'' added.` (green, `autoreply_added` — note the doubled single-quote is literal, not an escape); `/uchat autoreply list` immediately afterward shows a `greeting` row with keyword `greeting`, mode `contains`, response `hello` | server | |
| ultichat.autoreply.add.neg-duplicate | `language: en` in config.yml; a rule named `greeting` already exists (run `ultichat.autoreply.add` first) | Run `/uchat autoreply add greeting bye` | Chat/console line reads `Rule ''greeting'' already exists.` (red, `autoreply_exists`); `/uchat autoreply list` immediately afterward still shows `greeting`'s ORIGINAL keyword/response (`hello`) unchanged — `AutoReplyService#addRule` refuses rather than silently overwriting | server | |
| ultichat.autoreply.list | `language: en` in config.yml; the default two shipped rules (`server-ip`, `rules-info`) present, unmodified | Run `/uchat autoreply list` | Chat/console starts with `===== Auto-Reply Rules =====` (gold, `autoreply_list_header`), then one line per rule matching `autoreply_list_entry`'s template — for `server-ip`: `- server-ip -> server IP [contains] => Server address: play.example.com` (colorized per the template, not a paraphrase) | server | |
| ultichat.autoreply.list.neg-empty | `language: en` in config.yml. **This row deletes every auto-reply rule, and since `UltiKits/UltiChat#17` was fixed each deletion is written to `plugins/UltiTools/pluginConfig/UltiTools-Chat/config/autoreply.yml` as the command runs. Neither `/uchat reload` nor a restart brings the shipped rules back once they are gone, because the file then holds `rules: {}` and a present key always wins over the field default (measured); deleting the file outright and restarting is a different input and does restore them, but it would also discard whatever this batch has added, which is why this row copies the file aside instead. They cannot be recreated by command either, because `/uchat autoreply add <name> <response>` binds exactly one token to the response (`BaseCommandExecutor` refuses a longer argument list outright) and always sets keyword = name.** So, in the server's own shell and before anything else, copy the file aside: `cp plugins/UltiTools/pluginConfig/UltiTools-Chat/config/autoreply.yml autoreply.yml.bak` | (1) Run `/uchat autoreply list` and **write the listing down verbatim** — it is both what step 1 removes and what step 4 is compared against; it holds the two shipped defaults `server-ip`/`rules-info`, PLUS `greeting` if an earlier row in this dispatch batch already added it, which since `UltiKits/UltiChat#17` persists. Then run `/uchat autoreply remove <name>` against every rule that listing shows, directly, without depending on any other checklist row's own execution order for correctness; (2) run `/uchat autoreply list` — this step is the row's own assertion; (3) put the file back: `cp -f autoreply.yml.bak plugins/UltiTools/pluginConfig/UltiTools-Chat/config/autoreply.yml`, then run `/uchat reload`; (4) run `/uchat autoreply list` once more, then `rm autoreply.yml.bak` | (2) Chat/console shows `===== Auto-Reply Rules =====` (gold) followed immediately by `No auto-reply rules configured.` (gray, `autoreply_list_empty`) — no rule entry lines. (4) the listing is **identical, line for line, to the listing written down in step 1** — same rules, same keywords, same order, including `greeting` if step 1 showed it. That equality is the row's verification that step 3 restored the file rather than silently doing nothing; it is deliberately not a claim about which later rows the restore is sufficient for, because what it restores is the file as it stood when this row ran and not a shipped-default file — `ultichat.config.autoreply-yml` needs the latter and establishes it for itself | server | |
| ultichat.autoreply.persistence | `language: en` in config.yml; a rule `greeting` added via `/uchat autoreply add greeting hello` (run `ultichat.autoreply.add` first); the two shipped default rules `server-ip` and `rules-info` present, confirmed with `/uchat autoreply list` before step (a) — the Expected below uses them as its "the listing is not reading nothing" control, and `ultichat.autoreply.list.neg-empty` deletes them before putting them back, so run this row before that one or after its restore step | (a) Run `/uchat reload` (not a restart), then `/uchat autoreply list`. (b) Then stop the server completely — a full clean shutdown — start it again, and run `/uchat autoreply list` once more | (a) After the reload the listing still carries the line `- greeting -> greeting [contains] => hello` (colorized per `autoreply_list_entry`, not a paraphrase) — `AutoReplyService#addRule` saved `config/autoreply.yml` when the command ran, so the re-`init()` that `/uchat reload` performs reads the rule back rather than discarding it (UltiKits/UltiChat#17). (b) After the restart the listing carries that same line, unchanged. Both listings also still carry the two shipped default rules' lines (`server-ip`, `rules-info`), which is what distinguishes "the rule survived" from "the listing is reading nothing" | server | |
| ultichat.autoreply.persistence.neg-save-failure | `language: en` in config.yml; no rule named `unsaved` exists — confirm with `/uchat autoreply list` and keep that listing, it is what step 4 is compared against; the server process does not run as root — run `id -u` in the server's own shell as the user the server runs as, and confirm it does not print `0`, because root is not refused by the file mode this row relies on | In the server's own shell, as the server's own user: (1) `chmod 0444 plugins/UltiTools/pluginConfig/UltiTools-Chat/config/autoreply.yml`; (2) control step — run `printf 'x\n' >> plugins/UltiTools/pluginConfig/UltiTools-Chat/config/autoreply.yml` and confirm the shell itself refuses it, which establishes that the file really cannot be written before anything is asked of the module; (3) in-game or on the console run `/uchat autoreply add unsaved hello`; (4) run `/uchat autoreply list`; (5) restore the file with `chmod 0644 plugins/UltiTools/pluginConfig/UltiTools-Chat/config/autoreply.yml` | (2) the control write is refused with `Permission denied`. (3) the command replies `Rule ''unsaved'' could not be saved; the change was rolled back. See the server log.` (red, `autoreply_save_failed`) and does NOT reply `Auto-reply rule ''unsaved'' added.`. (4) the listing contains no `unsaved` line, and is otherwise identical to the listing kept in the preconditions. The server log carries a line containing `Could not save config/autoreply.yml after changing auto-reply rule 'unsaved'` | server | |
| ultichat.autoreply.remove | `language: en` in config.yml; a rule `greeting` exists (run `ultichat.autoreply.add` first) | Run `/uchat autoreply remove greeting` | Chat/console line reads `Auto-reply rule ''greeting'' removed.` (green, `autoreply_removed`); `/uchat autoreply list` immediately afterward no longer lists `greeting` | server | |
| ultichat.autoreply.remove.neg-not-found | `language: en` in config.yml; no rule named `does-not-exist` | Run `/uchat autoreply remove does-not-exist` | Chat/console line reads `Rule ''does-not-exist'' not found.` (red, `autoreply_not_found`) | server | |
| ultichat.autoreply.setkeyword | `language: en` in config.yml; a rule `greeting` exists with keyword `greeting` (run `ultichat.autoreply.add` first) — **this is this module's positive control (D-07): a prior mechanical extraction approach on this exact codebase silently dropped this row** | Run `/uchat autoreply setkeyword greeting howdy` | Chat/console line reads `Rule ''greeting'' keyword set to ''howdy''.` (green, `autoreply_keyword_set`); `/uchat autoreply list` immediately afterward shows `greeting`'s keyword as `howdy`, its response and mode UNCHANGED from before (`AutoReplyService#setKeyword` touches only the keyword field) | server | |
| ultichat.autoreply.setkeyword.neg-not-found | `language: en` in config.yml; no rule named `does-not-exist` | Run `/uchat autoreply setkeyword does-not-exist anything` | Chat/console line reads `Rule ''does-not-exist'' not found.` (red, `autoreply_not_found`) | server | |
| ultichat.autoreply.trigger | `language: en` in config.yml; `autoreply.enabled: true`; the default `server-ip` rule present (keyword `server IP`, mode `contains`, case-insensitive); the triggering player does NOT hold `ultichat.autoreply.bypass`; the player has not triggered any auto-reply within the last `autoreply.cooldown` seconds (default 10) | Send a chat message containing the substring `server IP` (any case, e.g. `what is the Server IP?`) | The player receives the rule's response, `Server address: play.example.com`, translated for `&`-codes (none present in this default response) | server | |
| ultichat.autoreply.trigger.neg-bypass-permission | Same as `ultichat.autoreply.trigger`, except the sending player holds `ultichat.autoreply.bypass` | Send a chat message containing the substring `server IP` | No auto-reply response is received — `AutoReplyListener#onPlayerChat` returns before matching when the sender holds the bypass permission | server | |
| ultichat.autoreply.trigger.neg-cooldown | Same as `ultichat.autoreply.trigger`, EXCEPT `anti-spam.enabled: false` (NOT the shipped default) — with anti-spam left on, its own 2-second cooldown would cancel the second message at `ChatListener#handleAntiSpam` (`LOWEST` priority) before `AutoReplyListener#onPlayerChat` (`MONITOR`, `ignoreCancelled = true`) ever sees it, making this row pass even if the auto-reply cooldown itself is broken; then the SAME player immediately triggers the rule again (within `autoreply.cooldown` seconds, default 10, of their first trigger) | Send a second chat message containing `server IP` immediately after the first | No second auto-reply response is received for the second message — `AutoReplyListener#isOnCooldown` refuses within the cooldown window (isolated from anti-spam by the precondition above) | server | |

## Channels

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultichat.channel.assign-on-join | `channels.enabled: true`; a RETURNING player (has played on this server before) who is currently switched to a non-default channel (e.g. ran `/ch local` in a prior session), then quits | The same player rejoins, then immediately runs `/ch list`, with NO `/ch` command run in between | The `Current channel:` line (see `ultichat.channel.list`) reads the configured `channels.default-channel` value (`global`), NOT `local` — `PlayerChannelListener#onPlayerJoin` calls `ChannelService#setPlayerChannel` UNCONDITIONALLY on every join, overwriting whatever channel (if any) the player held before, not merely assigning a default to a player with no prior assignment. This is the row's actual distinguishing signal: a fresh player showing the default on first join proves nothing on its own, since `ChannelService#getPlayerChannel`'s own `getOrDefault` fallback would produce the identical result even if this handler never ran at all; a RETURNING player being forced back to `global` from a different channel is the only observation `onPlayerJoin`'s own unconditional write, rather than the fallback, can produce | server | |
| ultichat.channel.cleanup-on-quit | `channels.enabled: true`; `anti-spam.enabled: true`, `anti-spam.max-duplicate: 3`, `anti-spam.cooldown: 2` and `anti-spam.duplicate-window: 0` (no time limit; all fresh defaults); the test player does NOT hold `ultichat.spam.bypass` | 1. As the test player, send `same line again` three times about 3 seconds apart, then a fourth time about 3 seconds later. 2. Quit, rejoin within 60 seconds, wait 3 seconds, and send `same line again` once more. 3. Read `PlayerChannelListener.java` | Step 1: the first three are delivered and the fourth is refused as a duplicate (the duplicate refusal text returned by `AntiSpamService#checkSpam`, Chinese, `UltiKits/UltiChat#18`) — the control that the history this row expects to disappear really exists and is enforced. Step 2: the message is delivered normally, although with no time window all three earlier copies would still count — only an emptied history explains it, so `PlayerChannelListener#onPlayerQuit` called `AntiSpamService#cleanup` (`UltiKits/UltiChat#20`). Step 3: the same handler calls `ChannelService#removePlayer`, removing the player's `playerChannels` entry; that half has NO independently observable signal through this module's command surface, because `PlayerChannelListener#onPlayerJoin` (see `ultichat.channel.assign-on-join`) unconditionally overwrites the entry on the next join whether or not it was removed, so it is confirmed from source only | server | |
| ultichat.channel.gate | `channels.enabled: true` (shipped default) | Restart the server, then run `/ch help` | `/ch` is a recognized command — `ChannelCommands` was registered at boot because `channels.enabled` was `true` at component-scan time | server | |
| ultichat.channel.gate.neg-disabled | `channels.enabled: false` in `config/channels.yml`, set BEFORE the restart below (a `/uchat reload` alone does NOT apply this — `@ConditionalOnConfig` is evaluated once, at boot) | Restart the server, then run `/ch help` | `/ch` is NOT a recognized command (Bukkit's own "unknown command" response) — `ChannelCommands` was never registered because `channels.enabled` was `false` at component-scan time | server | |
| ultichat.channel.list | `language: en` in config.yml; `channels.enabled: true` (shipped default); the three shipped channels present (`global`, `local`, `staff`); the running player does NOT hold `ultichat.channel.staff` | Run `/ch list` | Chat starts with `===== Chat Channels =====` (gold, `channel_list_header`), lists exactly `global` and `local` (NOT `staff` — the player lacks its required permission) each formatted per `channel_list_entry` (e.g. `- [Global] (global)`), then ends with a `Current channel: <display name>` line (gray, `channel_current`) naming the player's actual current channel | server | |
| ultichat.channel.switch | `language: en` in config.yml; `channels.enabled: true`; the player currently in `global`, switching to `local` (which exists and carries no permission requirement) | Run `/ch local` | Chat line reads `Switched to channel: [Local]` (green, `channel_switched`, using the channel's `display-name`, not its raw id); `/ch list`'s `Current channel:` line afterward confirms `local` | server | |
| ultichat.channel.switch.neg-no-permission | `language: en` in config.yml; `channels.enabled: true`; the player does NOT hold `ultichat.channel.staff` | Run `/ch staff` | Chat line reads `You don''t have permission for channel staff.` (red, `channel_no_permission` — doubled single-quote literal); the player's current channel is UNCHANGED | server | |
| ultichat.channel.switch.neg-not-found | `language: en` in config.yml; `channels.enabled: true`; no channel named `does-not-exist` | Run `/ch does-not-exist` | Chat line reads `Channel ''does-not-exist'' not found.` (red, `channel_not_found` — doubled single-quote literal) | server | |

## Chat message pipeline

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultichat.chat.pipeline | `chat.format-enabled: true`, `mentions.enabled: true`, `anti-spam.enabled: true`, `channels.enabled: true` (all shipped defaults); sender not on anti-spam cooldown; sender in the `global` channel; sender does NOT hold `ultichat.spam.bypass` (see `.neg-spam-bypass` below for the opposite); sender holds `ultichat.color`/`ultichat.emoji` if those slices are to be exercised in the same pass; PlaceholderAPI NOT installed; at least two OTHER players online, ALL in the `global` channel — one of them is the mention target, the other is a plain recipient; a THIRD player is online in a different channel (e.g. `local`), for `.neg-channel-isolation` below. The mention target must be in `global`, the SAME channel as the sender — `processMentions` plays `mentions.sound` to any online player whose name is mentioned regardless of channel, but the highlighted TEXT only reaches a player who is also a chat recipient, so mentioning the off-channel `local` player would hear the sound without ever seeing the replaced token, a different (and untested-by-this-row) case | Send a chat message containing `@<the global-channel mention target's name>` | The message is delivered to every recipient in `global` (channel-scoped filtering, including the mention target and the plain recipient) but NOT to the `local`-channel player (see `.neg-channel-isolation` below); the delivered line reads exactly `[Global] [%player_world%] <player>: <message>` (colors per the `channels.yml` `global` channel's `display-name` and `chat.yml`'s `chat.format`) — `ChatListener#applyChatFormat` prepends the channel's own display name ahead of the configured `chat.format` string, and `%player_world%` appears as LITERAL, unresolved text (not a world name): with PlaceholderAPI absent, `escapeFormatString` turns each of its two `%` characters into a literal `%%` -> `%` pair rather than a `String.format` conversion, so the token passes through unresolved; the mentioned player's client plays the configured `mentions.sound` AND sees the `@name` token replaced per `mentions.format` (both effects observable, since the mention target is a chat recipient here) | server | |
| ultichat.chat.pipeline.neg-spam-bypass | Same as `ultichat.chat.pipeline`, except the sending player holds `ultichat.spam.bypass`, AND the player is currently within `anti-spam.cooldown` of a previous message (a condition that would otherwise trigger `.neg-antispam-block` below) | Send a second chat message immediately after a first, as the bypass-permission holder | The message is delivered normally (not cancelled) — `ChatListener#handleAntiSpam` returns before calling `AntiSpamService#checkSpam` OR `#recordMessage` at all when the sender holds `ultichat.spam.bypass`, so cooldown, duplicate-detection, and caps-limit enforcement are all skipped for this sender, not just the cooldown check | server | |
| ultichat.chat.pipeline.neg-antispam-block | `anti-spam.enabled: true`; the sending player is currently within `anti-spam.cooldown` seconds of their previous message (trigger one ordinary message first, then immediately send a second) | Send a second chat message immediately after the first | The event is cancelled (`AsyncPlayerChatEvent#setCancelled(true)`) — no other player receives the message at all, not even in unformatted form; the sender alone receives a refusal line in red — the exact literal string returned by `AntiSpamService#checkCooldown` (not reproduced in this English-only document per D-02; read the cited source line for the exact characters), hardcoded Simplified Chinese regardless of the server's `language` setting, a known product defect, `UltiKits/UltiChat#18` | server | |
| ultichat.chat.pipeline.neg-channel-isolation | Same as `ultichat.chat.pipeline`'s positive case | (same message as `ultichat.chat.pipeline`) | The recipient in the `local` channel does NOT receive the message at all — `ChatListener#onChat` cleared `event.getRecipients()` and re-added only the filtered (same-channel) set before any other player received the event | server | |

## Join/Quit Messages

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultichat.joinquit.on-join | `language: en` in config.yml; `join-quit.join-message-enabled: true`, `join-quit.welcome-enabled: true`, `join-quit.title.enabled: true` (shipped defaults); a player UUID that has genuinely never joined this server before (`Player#hasPlayedBefore()` is false); PlaceholderAPI NOT installed — `JoinQuitListener#parsePlaceholders` delegates entirely to `PlaceholderAPI#setPlaceholders` when PAPI IS present and skips its own `%player_name%`/`%online_players%`/`%max_players%` fallback substitutions, so an installed-but-missing-expansion state would leave these tokens unresolved and this row's exact-text assertions non-deterministic | The player joins for the first time | The server-wide join broadcast reads `[+] <player> joined the server` (green `[+]`, yellow name, gray text, per `join-quit.join-message-format`); the joining player alone receives the 4 configured welcome lines; the joining player alone sees a title `Welcome Back` / subtitle `<player>`; the WHOLE SERVER additionally sees the first-join broadcast `Welcome new player <player>!` (gold) | server | |
| ultichat.joinquit.on-join.neg-not-first-join | Same config as `ultichat.joinquit.on-join` (including PlaceholderAPI NOT installed); a player who has already played on this server before (`hasPlayedBefore()` is true) | The player joins again | The join broadcast, welcome lines, and title all still appear exactly as in the positive case; the server-wide first-join broadcast (`Welcome new player...`) does NOT appear — `hasPlayedBefore()` gates only that one sub-behaviour | server | |
| ultichat.joinquit.on-quit | `language: en` in config.yml; `join-quit.quit-message-enabled: true` (shipped default); PlaceholderAPI NOT installed, for the same determinism reason as `ultichat.joinquit.on-join` (`join-quit.quit-message-format` also uses `%player_name%`) | The player quits (disconnect, not kick) | The server-wide quit broadcast reads `[-] <player> left the server` (red `[-]`, yellow name, gray text, per `join-quit.quit-message-format`) | server | |

## Scheduled Broadcasts

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultichat.announce.bossbar-broadcast | `announcements.bossbar.enabled: true` (NOT the shipped default — flip it on for this row); at least one player online. The period is fixed at 1200 ticks (60 s); no setting shortens it (`UltiKits/UltiChat#13`) | Wait 60 seconds with at least one player online | A boss bar titled `Welcome to the server!` (the shipped default message, translated), in the configured color (`BLUE` by default), appears to every online player, then disappears automatically after `announcements.bossbar.duration` seconds (default 10) | server | |
| ultichat.announce.chat-broadcast | `announcements.chat.enabled: true` (shipped default); at least one player online. The period is fixed at 6000 ticks (300 s); no setting shortens it (`UltiKits/UltiChat#13`) | Wait 300 seconds with at least one player online | Every online player receives a chat line beginning with the configured prefix `[Announcement] ` (gold/white), followed by one of the two shipped default messages, rotating on each successive firing | server | |
| ultichat.announce.title-broadcast | `announcements.title.enabled: true` (NOT the shipped default — flip it on for this row); at least one player online. The period is fixed at 12000 ticks (600 s); no setting shortens it (`UltiKits/UltiChat#13`) | Wait 600 seconds with at least one player online | Every online player sees a title `Welcome!` with subtitle `Enjoy your stay` — the shipped default message's double-vertical-bar separator correctly splits into title/subtitle (verifies `AnnouncementService#broadcastTitle`'s separator-index split, not just that some title appears) | server | |

## Data persistence

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultichat.channel.membership-not-persisted | none | Read `ChannelService.java` directly: `private final Map<UUID, String> playerChannels = new ConcurrentHashMap<>();` is a plain runtime field with no `@Table`, no `DataOperator`, and no file I/O anywhere in this class or its callers (separately grep this class for each of `@Table`, `DataOperator`, and `getOperator` — each returns 0 hits) | No persistence mechanism exists for `playerChannels` at all — the map is reconstructed empty on every server start. This is deliberately a static source check, not a live restart-and-rejoin test: `PlayerChannelListener#onPlayerJoin` (see `ultichat.channel.assign-on-join`) unconditionally overwrites the map entry on every join regardless of what a prior session held, so a live test observing "channel reverts to default after restart" cannot distinguish "never persisted" from "persisted successfully, then immediately overwritten by the join handler's own unconditional default-assignment" — the two causes are indistinguishable from `/ch list` output alone | protocol | |

## Module lifecycle

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultichat.lifecycle.duplicate-window-warning | Server stopped. In `plugins/UltiTools/pluginConfig/UltiTools-Chat/config/chat.yml`, set `anti-spam.duplicate-window: 60` — the value every server that ran an earlier version already holds, because earlier versions wrote their default of 60 into the file | 1. Start the server and read the console. 2. Change the value to `0`, save, run `/uchat reload` from the console and read the console | Step 1 prints exactly one WARNING line containing `UltiChat: `, the path of the `chat.yml` it read, `'anti-spam.duplicate-window' to 60 seconds`, `more permissive than before the upgrade`, `set it to 0 (no time limit, the new default)` and `/uchat reload`, and ending with `(UltiKits/UltiChat#14).` Step 2 prints no line containing `'anti-spam.duplicate-window'`. Step 1 is this row's positive control and must be read first: without it, step 2's silence is indistinguishable from a check that never fires (`UltiKits/UltiChat#14`) | server | |
| ultichat.lifecycle.removed-key-warning | Server stopped. In `plugins/UltiTools/pluginConfig/UltiTools-Chat/config/announcements.yml`, hand-add the three keys this version removed from it, exactly as an upgraded install still carries them: `interval: 300` inside the `announcements.chat` block, `interval: 60` inside `announcements.bossbar`, `interval: 600` inside `announcements.title`. In `config/chat.yml` in the same folder, hand-add `mute-duration: 30` inside the `anti-spam` block | 1. Start the server and read the console. 2. Run `/uchat reload` from the console and read the console. 3. Stop the server, delete the three `interval` lines and the `mute-duration` line, save both files, start the server again and read the console | Step 1 prints exactly four WARNING lines containing the text `which this version no longer reads`, one per key, each containing `UltiChat: `, the path of the file it read, and the key's own dotted name in single quotes, and each ending with `Delete the key from the file to silence this warning.` The three from `announcements.yml` come first (`'announcements.chat.interval'`, `'announcements.bossbar.interval'`, `'announcements.title.interval'`), each naming its fixed period (`every 300 seconds`, `every 60 seconds`, `every 600 seconds` respectively) and `UltiKits/UltiTools-Reborn#531`; the fourth, from `chat.yml`, names `'anti-spam.mute-duration'`, says players were `never muted automatically`, and cites `UltiKits/UltiChat#30`. Step 2 prints the same four lines again. Step 3 prints NONE of them. Step 1 is this row's positive control and must be read first: without it, step 3's silence is indistinguishable from a check that never fires (`UltiKits/UltiChat#13`, `UltiKits/UltiChat#15`) | server | |

## Configuration

One row per shipped yml file (D-06's config-per-file rule), not per key: `announcements.yml`
(12 keys), `autoreply.yml` (3 keys), `channels.yml` (3 keys), `chat.yml` (21 keys), `emojis.yml`
(2 keys) — 41 keys total, matching `FEATURES.md`'s `## Configuration` section exactly. Each row
confirms every key in the file is present at its `FEATURES.md`-documented default, then flips one
or more representative keys and observes the behaviour follow. No key in these five files is
documented as having no observable effect any more: the four that were are deleted
(`UltiKits/UltiChat#13`, `#15`) and `anti-spam.duplicate-window` is now read (`UltiKits/UltiChat#14`).

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultichat.config.announcements-yml | Fresh `config/announcements.yml` at its shipped default | Load the file; confirm all 12 keys listed under `FEATURES.md`'s `## Configuration` section are present at their documented defaults, and that none of the three removed keys `announcements.chat.interval`, `announcements.bossbar.interval`, `announcements.title.interval` is present; then set `announcements.bossbar.enabled: true` and `announcements.bossbar.color: RED` (default `BLUE`), restart, wait for one boss-bar firing (see `ultichat.announce.bossbar-broadcast`); separately set `announcements.bossbar.duration: 3` (default 10) and confirm the bar disappears sooner | All 12 keys present at their documented defaults, and no `interval` key anywhere in the file, before the change; after the change, the boss bar appears in RED rather than BLUE, and disappears after approximately 3 seconds rather than 10 | protocol | |
| ultichat.config.autoreply-yml | Fresh `config/autoreply.yml` at its shipped default — `rules` holding the two default entries `server-ip`/`rules-info` and nothing else. **Establish it, do not assume it:** since `UltiKits/UltiChat#17` every `/uchat autoreply add` and `setkeyword` writes to this file as the command runs, so by the time the auto-reply rows above have run it also holds `greeting`. Delete the file (`rm plugins/UltiTools/pluginConfig/UltiTools-Chat/config/autoreply.yml`) and **restart the server** — not `/uchat reload`: a reload re-initialises the entity that is already in memory, and a missing key is written back from that entity's current field value, so the deleted file comes back holding whatever the auto-reply rows left in memory (measured: `[greeting, server-ip, rules-info]`). A restart re-extracts the shipped `config/autoreply.yml` from the module jar before reading it (`UltiToolsPlugin#saveResources()`, which `/uchat reload` does not call), so the recreated file holds exactly the two shipped entries (measured; the jar entry is 376 bytes). Note what the `rm` and the restart rest on: the clean shutdown in between runs `ConfigManager#saveAll`, which writes only an entity modified since its last snapshot — the last rule command already saved and re-snapshotted, so the entity is clean here and the shutdown does not recreate the file from memory before the jar copy is extracted. Confirm the result with `/uchat autoreply list` before step 1. Also `anti-spam.enabled: true` (shipped default, left as-is) | Load the file; confirm all 3 keys (`enabled`, `cooldown`, `rules`) are present at their documented defaults (`rules` holding exactly the 2 shipped entries); then set `autoreply.cooldown: 1` (default 10) and send two triggering messages roughly 3 seconds apart — past `anti-spam.cooldown`'s own 2-second window (so `ChatListener#handleAntiSpam` does not cancel the second message before `AutoReplyListener` ever sees it) but well inside the OLD 10-second auto-reply cooldown window — and confirm both receive a response | All 3 keys present at their documented defaults before the change; after lowering `autoreply.cooldown` to 1s, a second trigger sent ~3 seconds later (past both the anti-spam cooldown and the NEW 1s auto-reply cooldown, but still inside the OLD 10s auto-reply cooldown) DOES receive a response, proving the lowered value took effect rather than merely surviving anti-spam's own shorter cooldown | protocol | |
| ultichat.config.channels-yml | Fresh `config/channels.yml` at its shipped default (three channels: `global`, `local`, `staff`) | Load the file; confirm all 3 keys (`enabled`, `default-channel`, `channels`) are present at their documented defaults, with the `channels` map holding exactly the 3 shipped channel definitions; then set `channels.default-channel: local` (default `global`), restart, and have a fresh player join. Do NOT test any channel's own `format` sub-field for an observable effect on the outgoing chat line — it is declared but never applied (`UltiKits/UltiChat#16`) | All 3 keys present at their documented defaults before the change; after the change, a freshly-joining player's `/ch list` shows `local` as their `Current channel:`, not `global` | server | |
| ultichat.config.chat-yml | Fresh `config/chat.yml` at its shipped default | Load the file; confirm all 21 keys listed under `FEATURES.md`'s `## Configuration` section are present at their documented defaults, and that the removed key `anti-spam.mute-duration` is not present; then exercise each of this file's five independently-toggled sub-areas with a value that differs from its own default, in separate configurations so no toggle masks another's effect: (a) set `chat.format-enabled: false` (default true), restart, send a chat message, and confirm the message is NOT run through the `chat.format` template (Bukkit's own default `<player> message` shape instead); (b) set `mentions.self-mention: true` (default false), send a message mentioning yourself, and confirm the mention highlight/sound now applies to a self-mention (it did not before the change); (c) set `join-quit.welcome-enabled: false` (default true), have a fresh player join, and confirm no welcome lines are sent (join broadcast and title, still enabled, still appear); (d) set `anti-spam.caps-limit: 10` (default 70), send the mixed-case message `Hey THIS rocks today` (29% uppercase by letter count — passes the default 70% limit, fails the lowered 10% limit; do NOT use an all-caps message, which would be refused identically at both limits and prove nothing about the change) and confirm it is now refused where it previously was not; (e) with `anti-spam.duplicate-window` at its fresh default 0 (no time limit), send `hello there` four times about 3 seconds apart (past the 2-second `anti-spam.cooldown`), then set `anti-spam.duplicate-window: 30`, run `/uchat reload`, send `hello again` four times about 3 seconds apart, wait 40 seconds, and send `hello again` once more | All 21 keys present at their documented defaults, and no `anti-spam.mute-duration`, before any change; (a) the format-disabled message reaches players in Bukkit's own default shape, not the configured template; (b) the self-mention now highlights and plays a sound where it previously did not; (c) no welcome lines are sent while the join broadcast and title both still are; (d) `Hey THIS rocks today` is delivered normally at the default 70% limit, then refused (anti-spam Chinese refusal text, `UltiKits/UltiChat#18`) at the lowered 10% limit; (e) at 0, the fourth `hello there` is refused as a duplicate (the duplicate refusal text returned by `AntiSpamService#checkSpam`, Chinese, `UltiKits/UltiChat#18`) — the control that detection is on; at 30, the fourth `hello again` (about 9 seconds after the first, well inside the window) is refused the same way — the control that the lowered window has not switched detection off — and the fifth, sent about 50 seconds after the first so that all three retained copies are older than 30 seconds, is delivered normally | protocol | |
| ultichat.config.emojis-yml | Fresh `config/emojis.yml` at its shipped default (4 mappings: `:heart:`, `:star:`, `:smile:`, `:sword:`) | Load the file; confirm both keys (`enabled`, `mappings`) are present at their documented defaults, with `mappings` holding exactly the 4 shipped shortcodes; then add a new mapping `":wave:": "👋"`, restart, and send a chat message containing `:wave:` from a player holding `ultichat.emoji` | Both keys present at their documented defaults before the change; after the change, the outgoing chat line shows `👋` in place of the literal `:wave:` text | server | |
